# Express Abu Sayed Android
Android Studio project prepared for building an APK.

Open this folder in Android Studio and Build > Build APK(s).
The app includes WebView assets and Android location permission/native GPS bridge.
The current web UI was copied from the Express Abu Sayed project.

A true multi-device live tracking system still requires a hosted backend/API; this Android wrapper alone does not create that server.
